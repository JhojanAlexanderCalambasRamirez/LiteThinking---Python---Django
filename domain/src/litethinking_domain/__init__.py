"""
litethinking_domain - Pure domain layer.

No framework dependencies. No Django. No SQLAlchemy. No HTTP.
Only Python stdlib + business rules.

Consumed by backend services via Poetry dependency.
"""

__version__ = "0.1.0"
